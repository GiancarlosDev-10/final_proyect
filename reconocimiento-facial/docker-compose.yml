# Solo para probar el servicio en local. En el VPS se levanta desde
# deploy/contabo/docker-compose.yml (detrás de Caddy con HTTPS).
services:
  reconocimiento:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5000:5000"
