MONGODB_URI=
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000
# Secreto compartido con el workflow de n8n del bot de Telegram; se envía como header "x-api-key".
TELEGRAM_API_SECRET=
# Secreto compartido con el script de Python de reconocimiento facial; se envía como header "x-api-key".
ASISTENCIA_API_SECRET=
# URL pública del servicio de Python de reconocimiento (túnel de Cloudflare en la VPS).
# Expuesta al navegador (NEXT_PUBLIC_) porque la cámara le habla directo, sin pasar por Next.js.
NEXT_PUBLIC_RECONOCIMIENTO_URL=
